- Cool Mail Config to SSRF

- Unlikely but something new CSRF, where if email is still unverfied and victim visits attacker page, email account gets changed.

- Simple File Upload, stored HTML(XSS) and  RCE(php)

- LFI to RCE via fd, since it was opening log files and process was in running state.

- Simple numeric IDOR. Leads to deletion of imporant information, Unauthorized.

- Even After secondary mail is unlinked, its session remains, with which an attacker is able to change the secondary mail once again and retain persistence over an account. -> Writeup

- Present P4.
